apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: knative-simple
  namespace: default
spec:
  template:
    spec:
      containers:
        - image: docker.io/nropatas/knative-simple
